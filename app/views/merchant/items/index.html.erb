<section class="grid-container item-list">
  <% @merchant.items.each do |item| %>
    <section class="grid-item" id='item-<%=item.id%>'>
      <h2> <%=link_to item.name, "/items/#{item.id}" %> </h2>
      <p>Sold by: <%=link_to item.merchant.name, "/merchants/#{item.merchant.id}" %></p>
      <%=link_to image_tag(item.image), "/items/#{item.id}", id: "item-image-#{item.id}" %>
      <p> <%= item.description %> </p>
      <p>Price: <%=number_to_currency(item.price) %> </p>
      <p>Inventory: <%= item.inventory %> </p>
      <% if !@merchant %>
      <% end %>
      <% if item.active? %>
        <p>Active</p>
        <%= link_to "Deactivate", merchant_items_update_path(item, active?: false), method: :patch %>
      <% else %>
        <p>Inactive</p>
        <%= link_to "Activate", merchant_items_update_path(item, active?: true), method: :patch %>
      <% end %>
      <% if item.deletable? %>
        <%= link_to "Delete",  item_destroy_path(item) %>
      <% end %>
    </section>
  <% end %>
</section>